import QtQuick

Rectangle {
    anchors.fill: parent
    color: "#0d0d0d"

    Text {
        anchors.centerIn: parent
        text: "TASC Autonomy Workspace\nArrange camera docks as needed"
        color: "#2a2a2a"
        font.pointSize: 24
        horizontalAlignment: Text.AlignHCenter
    }
}